We analyze US gun murders data collected by the FBI

Download-data.R downloads csv (raw data) to data directory

wrangle-data.R creates a derived dataset and saves it as R object(.rda) in rda directory

analysis.R - generates visuals and saves it to figs directory
